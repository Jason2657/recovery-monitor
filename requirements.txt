# Core
anthropic>=0.44.0
pydantic>=2.0.0
rich>=13.7.0
python-dotenv>=1.0.0

# Web app (FastAPI + SSE + Deepgram voice)
fastapi>=0.115.0
uvicorn>=0.32.0
httpx>=0.27.0

# Fetch.ai — the agents as real message-passing uAgents
uagents>=0.12.0

# Arize / Phoenix — observability tracing + the self-correction loop.
# arize-phoenix-otel provides phoenix.otel.register; the openinference
# instrumentor auto-traces Anthropic calls. Install the full `arize-phoenix`
# package and run `phoenix serve` to view traces in the local UI.
arize-phoenix-otel>=0.6.0
openinference-instrumentation-anthropic>=0.1.6
